packages:
  - vim
  - tmux

vim_paths:
  - ~/.vim/autoload/
  - ~/.vim/plugins/

config_files:
  tmux:
    src: files/tmux.conf
    dest: ~/.tmux.conf

  ansible_nvaigator:
    src: files/ansible-navigator.yml
    dest: ~/.ansible-navigator.yml

  vim:
    src: files/vimrc
    dest: ~/.vimrc

